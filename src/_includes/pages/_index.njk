---
layout: layout/base.njk
colorHeader: has-light-menu
---

<article class="main-article lex-page">

  {# Hero split (título + foto + tarjetas flotantes + stats) #}
  {% include "templates/lex-hero-home.njk" %}

  {# Áreas de práctica (6 cards · 3 columnas) #}
  {% include "templates/lex-servicios.njk" %}

  {# Bloque navy CEE / Discapacidad #}
  {% include "templates/lex-cee.njk" %}

  {# Equipo (placeholders si no hay global.team) #}
  {% include "templates/lex-equipo.njk" %}

  {# Testimonios (solo se renderiza si hay reseñas reales en global.testimonios) #}
  {% include "templates/lex-testimonios.njk" %}

  {# Blog (3 últimos posts) #}
  {% include "templates/lex-blog.njk" %}

  {# FAQ split #}
  {% include "templates/lex-faq.njk" %}

  {# Contacto (info + formulario) #}
  {% include "templates/lex-contacto.njk" %}

</article>

{% include "templates/lex-styles.njk" %}
